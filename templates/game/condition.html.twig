{% extends 'base.html.twig' %}

{% block title %}MicroDonjon{% endblock %}

{% block body %}
<style>

</style>

<div class="example-wrapper">
    <h1>- {{chapitre.zone}} -</h1>

    {% if condition %}
        <p>{{chapCondition.texteSiVrai}}</p>

        {% for sortie in chapCondition.sorties %}
            {% if sortie.conditionVrai %}
                <a href="{{path("routing", {'id': sortie.chapitre.id})}}">{{sortie.texteLien}}</a><br><br>
            {% endif %}
        {% endfor %}

    {% else %}
        <p>{{chapCondition.texteSiFaux}}</p>
        
        {% for sortie in chapCondition.sorties %}
            {% if not sortie.conditionVrai %}
                <a href="{{path("routing", {'id': sortie.chapitre.id})}}">{{sortie.texteLien}}</a><br><br>
            {% endif %}
        {% endfor %}   
    {% endif %}
    
    
</div>
{% endblock %}