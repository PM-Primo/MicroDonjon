{% extends 'base.html.twig' %}

{% block title %}MicroDonjon{% endblock %}

{% block body %}
<style>

</style>

<div class="example-wrapper">
    <h1>- {{chapitre.zone}} -</h1>

    <p>{{chapStandard.texteChapitre | raw}}</p>

    {% if app.user.PVactuels > 0 %}
        {% for sortie in chapStandard.sorties %}
            <a href="{{path("routing", {'id': sortie.chapitre.id})}}">{{sortie.texteLien}}</a><br><br>
        {% endfor %}
    {% else %}
        Hélas, vos blessures risquent de mettre un terme à vos aventures. Votre vision se trouble.<br><br>
        <a href="{{path("game_over")}}">Vous vous sentez défaillir.</a><br><br>
    {% endif %}

    PV : {{app.user.PVactuels}} / {{app.user.PVmax}}<br>
    Or : {{app.user.gold}}<br>
    Attaque : {{app.user.attaque}}<br>

    Inventaire : 
    {% for item in app.user.inventaire %}
        {{item}} / 
    {% endfor %}

</div>
{% endblock %}