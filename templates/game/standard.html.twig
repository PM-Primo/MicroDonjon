{% extends 'base.html.twig' %}

{% block title %}MicroDonjon{% endblock %}

{% block body %}
<style>

</style>

<div class="game__right-page-content">
    
    <h1>{{chapitre.zone}}</h1>

    <p>{{chapStandard.texteChapitre | raw}}</p>

    {% if app.user.PVactuels > 0 %}
        <div class="game__choices">
            {% for sortie in chapStandard.sorties %}
                <a href="{{path("routing", {'id': sortie.chapitre.id})}}"  class="links__choice">{{sortie.texteLien}}</a><br><br>
            {% endfor %}
        </div>
    {% else %}
        Hélas, vos blessures risquent de mettre un terme à vos aventures. Votre vision se trouble.<br><br>
        <a href="{{path("game_over")}}" class="links__choice">Vous vous sentez défaillir.</a>
    {% endif %}

</div>
{% endblock %}